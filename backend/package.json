{
  "name": "ingenero360ai-server",
  "version": "1.0.0",
  "description": "Backend Node.js API service for plant optimizer",
  "main": "server.js",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon --ignore \"**/*.xlsx\" --ignore \"**/*.csv\" --ignore \"data/\" --ignore \"**/*.json\" server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.19.2",
    "xlsx": "^0.18.5"
  },
  "devDependencies": {
    "nodemon": "^3.1.0"
  }
}
